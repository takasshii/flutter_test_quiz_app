class LearningDataGet {
  LearningDataGet(
      this.currentContinuousRecord,
      this.continuousRecord,
      this.totalDay,
      this.todayTime,
      this.totalQuestion,
      this.learnedQuestion,
      this.totalLearningTime,
      this.createdAt,
      this.updatedAt);
  int currentContinuousRecord;
  int continuousRecord;
  int totalDay;
  int todayTime;
  int totalQuestion;
  int learnedQuestion;
  int totalLearningTime;
  DateTime createdAt;
  DateTime updatedAt;
}
