import 'package:flutter/material.dart';

class BookListTagVertical extends StatelessWidget {
  const BookListTagVertical({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
